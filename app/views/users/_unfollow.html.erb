<%= form_with(model: @user, scope: current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
